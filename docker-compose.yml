version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      # Add other env variables here

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
